.study-hero {
  --study-hero-bar-height: 72rpx;
  --study-hero-progress-top: 64rpx;
  --study-hero-progress-width: 180rpx;
  --study-hero-heading-margin-top: 10rpx;
  --study-hero-heading-padding-x: 0;
  --study-hero-heading-padding-bottom: 0;
  --study-hero-heading-justify: flex-start;
  position: relative;
  display: flex;
  flex-direction: column;
  width: 100%;
  min-width: 0;
}

.study-hero__bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  height: var(--study-hero-bar-height);
  min-height: var(--study-hero-bar-height);
  padding: 0;
  box-sizing: border-box;
  flex: 0 0 var(--study-hero-bar-height);
}

.study-hero__back,
.study-hero__actions {
  display: flex;
  align-items: center;
  width: 90rpx;
  height: var(--study-hero-bar-height);
  flex: 0 0 90rpx;
}

.study-hero__back {
  justify-content: flex-start;
}

.study-hero__actions {
  justify-content: flex-end;
}

.study-hero__center {
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 0;
  height: var(--study-hero-bar-height);
  flex: 1;
}

.study-hero__progress {
  position: absolute;
  left: 50%;
  top: var(--study-hero-progress-top);
  width: var(--study-hero-progress-width);
  height: 4rpx;
  transform: translateX(-50%);
  border-radius: var(--radius-pill);
  background: var(--study-progress-track, rgba(26, 26, 26, 0.08));
  overflow: hidden;
}

.study-hero__progress-fill {
  height: 100%;
  border-radius: inherit;
  background: var(--study-progress-fill, rgba(230, 57, 70, 0.78));
  box-shadow: 0 0 12rpx var(--study-progress-glow, rgba(230, 57, 70, 0.18));
  transition: width var(--dur-base) var(--ease-out);
}

.study-hero__heading {
  display: flex;
  justify-content: var(--study-hero-heading-justify);
  width: 100%;
  min-width: 0;
  margin-top: var(--study-hero-heading-margin-top);
  padding: 0 var(--study-hero-heading-padding-x) var(--study-hero-heading-padding-bottom);
}
