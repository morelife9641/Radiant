@import "styles/tokens.wxss";
@import "styles/animations.wxss";

page {
  background: linear-gradient(
    180deg,
    var(--surface-canvas-top) 0%,
    var(--surface-canvas-mid) 42%,
    var(--surface-canvas-bottom) 100%
  );
  color: var(--color-ink);
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", Helvetica, Arial, "PingFang SC", sans-serif !important;
  font-synthesis: none !important;
  -webkit-font-smoothing: antialiased;
  font-size: var(--font-body);
  line-height: 1.6;
}

view, text, button, input, textarea, label, picker {
  box-sizing: border-box;
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", Helvetica, Arial, "PingFang SC", sans-serif !important;
  font-synthesis: none !important;
  -webkit-font-smoothing: antialiased;
}

.page {
  min-height: 100vh;
  padding: 0 var(--page-padding-x);
}

.h1 { font-size: var(--font-h1); font-weight: 700; line-height: 1.2; }
.h2 { font-size: var(--font-h2); font-weight: 600; line-height: 1.3; }
.h3 { font-size: var(--font-h3); font-weight: 600; line-height: 1.4; }
.caption { font-size: var(--font-caption); color: var(--color-graphite); }
.muted { color: var(--color-mist); }

.card {
  background: var(--surface-card);
  border: 1rpx solid var(--border-subtle);
  border-radius: var(--radius-card);
  box-shadow: var(--shadow-soft);
  padding: var(--space-4);
}

.row { display: flex; align-items: center; }
.row-between { display: flex; align-items: center; justify-content: space-between; }
.col { display: flex; flex-direction: column; }
.gap-1 { gap: var(--space-1); }
.gap-2 { gap: var(--space-2); }
.gap-3 { gap: var(--space-3); }
.gap-4 { gap: var(--space-4); }

.safe-top { padding-top: env(safe-area-inset-top); }
.safe-bottom { padding-bottom: env(safe-area-inset-bottom); }
