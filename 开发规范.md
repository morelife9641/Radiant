# Radiant.AI 开发规范

> 一款将「英语学习」与「兴趣场景」结合的微信小程序。极简、克制、好用是第一原则。

---

## 一、产品定位

### 1.1 一句话定义
**Radiant.AI** —— 用你喜欢的游戏 / 你要考的试，学英语。

### 1.2 核心理念
- **极简优先**：每个页面只做一件事。能砍的功能就砍。
- **场景驱动**：词汇内容根据用户兴趣（游戏 / 备考）动态匹配。
- **AI 是搭子**：不是判分老师，是聊天对象、复盘伙伴。
- **轻量打卡**：每日任务式，3~5 分钟一次，不强求长时间使用。

### 1.3 目标用户
- 想学英语，但讨厌"教科书味"的年轻人。
- 玩游戏 / 看英文内容 / 备考的人。

---

## 二、引导流程（首次启动）

进入小程序后**必须依次完成 3 步**才能进入主界面，结果写入用户档案，可在「我的」中重新修改。

### Step 1 · 选择目的
两个大卡片二选一（也可多选其一作为主线）：

| 目的 | 说明 |
|---|---|
| **游戏交流** | 学游戏术语、英文社区表达、和队友开麦 |
| **备考** | 四六级 / 雅思 / 托福 / 考研 |

### Step 2 · 选择词书
根据 Step 1 的选择，展示对应可选词书列表（默认给出 4~6 本即可，未来可扩展）。

**游戏交流方向：**
- Dota 2 高频术语
- Valorant 战术沟通
- League of Legends 赛事英文
- 通用游戏开黑用语

**备考方向：**
- 大学英语四级核心词
- 大学英语六级核心词
- 雅思 5500
- 托福 TPO 高频
- 考研英语 5500

每本词书展示：封面色块 + 名称 + 词量 + 难度（A2/B1/B2）。
用户**只能同时启用一本**作为「当前词书」，但可以收藏多本随时切换。

### Step 3 · 选择兴趣场景
（仅当 Step 1 选了「游戏交流」时出现；选「备考」则跳过）
多选标签：`Dota 2 / Valorant / LoL / CS2 / Apex / 原神 / 影视剧`。
影响 AI 对话的话题倾向与每日任务的例句来源。

完成后进入主界面。

---

## 三、主界面（3 个 Tab）

```
┌─────────────────────────────┐
│  📅 今日       💬 对话      👤 我的  │
└─────────────────────────────┘
```

### Tab 1 · 今日（Home）
**就是每日任务页面，只展示今天要做的事。**

页面结构（自上而下，留白为主）：
1. **顶部问候**：「Hi, [昵称]」+ 今天日期。
2. **当前词书条**：一行小卡，显示当前词书名 + 进度（例如 `Dota 2 · 24 / 200`），右侧一个「切换」按钮 → 跳到词书库。
3. **今日任务卡（核心）**：
   - 大字「今日单词 · 10 个」
   - 进度环：`已完成 0 / 10`
   - 一个红色大按钮 `开始`
4. **复习卡**（如有 SRS 到期单词才显示）：
   - 「待复习 · X 个」+ 浅色按钮 `去复习`
5. **连击徽章**（可选小元素）：底部一行，「🔥 连续打卡 N 天 · 今日连击 ×5」。

**没了。** 不放推荐位、不放轮播、不放排行。

### Tab 2 · 对话（Chat）
**和 AI 用英文聊天，话题围绕用户兴趣场景。**

- 顶部：当前角色头像 + 名字（如「Sarah · Dota 队友」），右上一个齿轮可换角色 / 换话题。
- 中部：聊天气泡（AI 左白底，用户右红底白字）。
- 底部输入：
  - 主输入框（文本）
  - 左侧麦克风按钮（按住录音，松开发送）
  - 右侧发送按钮
- AI 回复后会**自动给出**：
  - 1 条「更地道的说法」建议（点击替换）
  - 不打分、不打断节奏

每天对话轮数有上限（成本控制，详见 §六）。

### Tab 3 · 我的（Profile）
极简列表页：
- 顶部：头像 + 昵称 + 当前等级（连续打卡天数）
- 列表项：
  - 词书库（切换 / 收藏词书）
  - 已掌握单词
  - 错词本
  - 设置（重新选目的 / 兴趣 / 反馈 / 关于）

**不做：技能树、徽章墙、排行榜、商店、装扮。**

---

## 四、学习玩法（极简）

### 4.1 每日单词任务
默认每天 10 个新词（用户可在设置中改 5 / 10 / 20）。

**单词卡流程：**
1. 显示英文 + 音标 + 听发音（自动播一次）。
2. 看中文释义和例句（例句来自当前兴趣场景，例：选了 Dota 2 就给「gank」「push」相关例句）。
3. 三个选项：
   - **认识** → 进入下一张
   - **模糊** → 加入今日复习池，再次出现
   - **不认识** → 标记为新词，加入 SRS

### 4.2 复习（SRS · 简化版）
SM-2 简化算法。每个单词的下次复习时间根据上次结果计算：
- 认识 → interval × 2，最长 30 天
- 模糊 → interval = 1 天
- 不认识 → interval = 10 分钟（当日内再次出现）

复习时只问一种题：**看英文，回想中文**，三选一（认识 / 模糊 / 不认识）。

### 4.3 连击
- **连击数**：今天连续答对（认识/模糊都算"过"）的单词数。
- **打卡连击**：连续打卡天数（每天至少完成 1 个单词任务即算）。

仅作为正反馈展示，不掉率、不复杂奖励。

---

## 五、设计语言（红 + 白）

### 5.1 配色
| 名称 | 色值 | 用途 |
|---|---|---|
| Radiant Red | `#E63946` | 主色，按钮、强调、连击 |
| Crimson Deep | `#B7202E` | 按下态、阴影、强调文字 |
| Cherry Light | `#FFE3E6` | 标签底、轻提示背景 |
| Pure White | `#FFFFFF` | 卡片、模态 |
| Off White | `#FAFAFA` | 页面底色 |
| Ink Black | `#1A1A1A` | 主文字 |
| Graphite | `#4A4A4A` | 次级文字 |
| Mist Gray | `#9CA3AF` | 占位、disabled |
| Hairline | `#E5E7EB` | 分割线 |

语义色：成功 `#10B981`，警告 `#F59E0B`，信息 `#3B82F6`。

### 5.2 字体
| 用途 | 字号(rpx) | 字重 |
|---|---|---|
| 大标题 H1 | 56 | 700 |
| 标题 H2 | 40 | 600 |
| 正文 | 28 | 400 |
| 辅助 | 24 | 400 |
| 数字（连击/进度） | 64+ | 900 |

中文 `PingFang SC`，英文 `SF Pro Rounded / Inter`。

### 5.3 形状 & 阴影
- 卡片圆角 `20rpx`，按钮圆角 `16rpx`，标签胶囊形 `999rpx`。
- 阴影只用一档：`0 6rpx 16rpx rgba(26,26,26,0.06)`，不堆叠。

### 5.4 间距
8 倍栅格：`8 / 16 / 24 / 32 / 48 / 64`。**页面左右安全边距统一 32rpx。**

### 5.5 微交互
- 按钮按下：`scale(0.96)`，120ms。
- 单词卡进入：从右滑入（`translateX(40rpx) → 0`，opacity 0→1），220ms。
- 答对反馈：✓ 在按钮中心淡入（仅视觉，无震动）。
- **禁止震动**：全局**禁用** `wx.vibrateShort` / `wx.vibrateLong`，任何反馈都不得调用。`utils/haptic.js` 中的方法保留为 no-op。
- 不要全屏特效、不要爆炸、不要 Lottie 烟花。极简。

---

## 六、技术栈

| 模块 | 选型 |
|---|---|
| 前端 | 微信原生小程序 + Skyline |
| 状态管理 | `mobx-miniprogram` + bindings |
| 后端 | 微信云开发（云函数 + 云数据库 + 云存储） |
| AI | DeepSeek-V3 主，通义千问 Plus 备，云函数代理 |
| 语音 | `wx.getRecorderManager` + 通义 ASR；TTS 用 Azure 或通义 |
| 动效 | CSS 为主，复杂动效再上 Lottie |

### AI 成本控制
- 对话：每用户每日 30 轮上限，单轮 prompt+completion ≤ 800 tokens。
- 超限提示「明天再聊」。

---

## 七、目录结构

```
miniprogram-3/
├── miniprogram/
│   ├── app.js / app.json / app.wxss
│   ├── pages/
│   │   ├── onboarding/
│   │   │   ├── purpose/      # Step 1 选目的
│   │   │   ├── wordbook/     # Step 2 选词书
│   │   │   └── interest/     # Step 3 选兴趣
│   │   ├── home/             # Tab 1 今日任务
│   │   ├── learn/            # 单词学习/复习页（从 home 进）
│   │   ├── chat/             # Tab 2 AI 对话
│   │   ├── profile/          # Tab 3 我的
│   │   ├── wordbook-list/    # 词书库（从 profile 或 home 切换按钮进）
│   │   └── settings/
│   ├── components/
│   │   ├── r-button/
│   │   ├── word-card/
│   │   ├── progress-ring/
│   │   ├── chat-bubble/
│   │   └── tag/
│   ├── styles/
│   │   ├── tokens.wxss       # 颜色 / 字号 / 间距变量
│   │   └── animations.wxss
│   ├── utils/
│   │   ├── request.js
│   │   ├── srs.js
│   │   ├── audio.js
│   │   └── haptic.js
│   ├── services/
│   │   ├── user.js
│   │   ├── wordbook.js
│   │   ├── learn.js
│   │   └── ai.js
│   ├── store/
│   ├── assets/
│   │   ├── icons/
│   │   ├── images/
│   │   └── sounds/
│   └── config/
│       └── index.js
│
└── cloudfunctions/
    ├── ai-chat/
    ├── learn-submit/         # 提交今日学习结果
    ├── user-sync/
    ├── wordbook-fetch/
    └── content-asr/          # 语音转文字代理
```

---

## 八、数据模型

```js
// users
{
  _id: string,                // openid
  nickname: string,
  avatar: string,
  purpose: 'gaming'|'exam',   // 用户目的
  interests: [string],        // ['dota2','valorant',...]
  activeWordbookId: string,   // 当前启用的词书
  savedWordbooks: [string],   // 收藏的词书
  dailyGoal: 10,              // 每日单词数
  streakDays: int,            // 连续打卡天数
  lastCheckInDate: date,
  createdAt: timestamp
}

// wordbooks（运营内容）
{
  _id: string,
  name: string,               // 'Dota 2 高频术语'
  category: 'gaming'|'exam',
  tags: [string],             // ['dota2'] 或 ['cet4']
  cefrLevel: string,
  totalWords: int,
  cover: string,              // 颜色或图片 URL
  description: string
}

// words
{
  _id: string,
  wordbookId: string,
  word: string,
  phonetic: string,           // 音标
  translation: string,
  partOfSpeech: string,
  audioUrl: string,
  examples: [
    { en: string, cn: string, scene: string }  // scene 用于兴趣过滤
  ]
}

// user_word_progress（每用户每词一条 SRS 记录）
{
  _id: string,
  userId: string,
  wordId: string,
  status: 'new'|'learning'|'mastered',
  easiness: float,            // 1.3 ~ 2.5
  interval: int,              // 分钟为单位
  nextReviewAt: timestamp,
  correctCount: int,
  wrongCount: int,
  updatedAt: timestamp
}

// chat_sessions（AI 对话历史，用于上下文）
{
  _id: string,
  userId: string,
  topic: string,              // 'dota2'|'free-talk'|...
  npcRole: string,            // 'Sarah'|'Coach'|...
  messages: [{ role:'user'|'assistant', content, ts }],
  tokenUsedToday: int,
  date: date
}

// ai_logs（成本追踪）
{ userId, endpoint, promptTokens, completionTokens, cost, createdAt }
```

---

## 九、业务规则

### 9.1 连击
- 单词连击：当日连续答对累加，答错重置为 0，仅当日有效。
- 打卡连击：今天完成 ≥1 个单词学习任务即记打卡，断签则归 0。

### 9.2 词书切换
- 切换词书不清空进度：每本词书的进度独立存储。
- 当前词书的 SRS 队列优先于其他词书。

### 9.3 防作弊
- 学习提交（点击认识/模糊/不认识）走 `learn-submit` 云函数，校验时间间隔（每词 ≥ 1.5s 才合理）。

---

## 十、开发约定

### 10.1 命名
- 文件 / 目录：`kebab-case`
- 变量 / 函数：`camelCase`
- 常量：`UPPER_SNAKE_CASE`

### 10.2 样式
- 使用 `tokens.wxss` 中的 CSS 变量，禁止裸写颜色 / 字号 / 圆角。
- 单位 `rpx`；动画时间 `ms`。

### 10.3 组件原则
- 单一职责，≤ 200 行。
- 命名前缀 `r-`（radiant），如 `r-button`、`r-word-card`。

### 10.4 提交规范
Conventional Commits：`feat / fix / style / refactor / perf / docs / chore`。

---

## 十一、性能底线

| 项 | 目标 |
|---|---|
| 首屏可交互 | < 1.5s |
| 页面切换 | < 300ms |
| 按钮反馈 | < 100ms |
| AI 首字延迟 | < 2s（流式） |
| 主包 | < 1.5MB |

分包：
- 主包：`onboarding / home / profile`
- 分包 A：`learn / wordbook-list`
- 分包 B：`chat`

---

## 十二、里程碑

| 阶段 | 周期 | 目标 |
|---|---|---|
| M0 基建 | 1 周 | tokens、公共组件、云函数骨架 |
| M1 引导 | 1 周 | 三步引导流程 + 词书选择 |
| M2 学习 | 2 周 | 今日任务 + 单词卡 + SRS |
| M3 对话 | 2 周 | AI 对话 + 语音 |
| M4 打磨 | 1 周 | 性能、动效、灰度 |
| M5 发布 | 1 周 | 上架审核 |

---

> 本文档为 SSOT。任何与代码/视觉冲突先改文档再写代码。**遇到"要不要加个功能"的问题，默认答案是不加。**
